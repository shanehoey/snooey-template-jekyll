# v1.2.135 _data/schema/breadcrumb.yml
# VNEXT - migrate to theme _data\schema

schema: &schema

  layout: &layout
    section: p-3
    background: bg-dark
    text: font-monospace text-light

dark: &dark
  <<: *schema

light: &light
  <<: *schema
  background: bg-light
  text: font-monospace text-dark

image: &image
  <<: *schema
  background: snooey-bg-teaser

default: 
  <<: *dark

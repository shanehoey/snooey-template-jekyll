# v1.2.110 _data/layout.yml

schema: &schema
    body: container
    header:
    content:
    footer:   

light: &light
    <<: *schema

dark: &dark
    <<: *schema

image: &image
    <<: *schema

theme:
    <<: *schema
    content: container mt-1 p-1